# Archived RedCubeToBox experts

This package preserves comparison implementations so their existing `--expert` names remain importable by the smoke
and batch runners. Use `autogen_polar_retreat_transport` for the current recommended workflow. Historical analysis is
kept in `selfmade/problem_solving.md`, not in this usage index.
