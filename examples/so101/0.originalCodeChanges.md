# OpenPI 原始代码改动记录（SO-101 / LeIsaac）

本文档集中记录本仓库为了完成 SO-101、LeIsaac、OpenPI π0.5 数据采集与训练链路而加入的改动，便于学习、复现、代码审查和日后回退。

## 记录边界

- 对比基线：`b730105`（`Change the video size of the libero output`）。
- 当前开发分支：`libero-repro`。
- 记录范围：从 `675a7a6` 开始加入的 SO-101/OpenPI 改动，以及本次训练退出清理修复。
- 基线之前的 LIBERO 改动不归入本文档，也不声称由本轮工作创建。
- Git 提交中的 author 是仓库配置的提交身份，不等同于本文档对代码来源的归属判断。

## 改动后的完整链路

```text
Windows SO-101 Leader (COM7)
  -> 本机串口读取、标定文件与安全 PID/力矩设置
  -> ZeroMQ 发布 tcp://127.0.0.1:5556
  -> SSH reverse tunnel
  -> 服务器 LeIsaac LiftCube 仿真与网页控制
  -> LeIsaac 原生 HDF5 成功轨迹
  -> 独立 OpenPI uv 环境转换为 LeRobot 0.4.2
  -> 计算 normalization statistics
  -> OpenPI pi0.5 SO-101 LoRA/full 训练
  -> WebSocket 策略服务返回 6 维 SO-101 动作
```

Isaac Sim 环境与 OpenPI/LeRobot 环境保持隔离。原因是 LeRobot 0.4.2 与已验证的 Isaac Sim 5.1 Python 依赖存在版本冲突，尤其是 `packaging`。因此仿真环境只负责录制原生 HDF5，转换与训练在 OpenPI 的 `uv` 环境中完成。

## 逐项代码改动

### 1. SO-101 策略适配器

文件：

- `src/openpi/policies/so101_policy.py`
- `src/openpi/policies/so101_policy_test.py`

主要行为：

- 定义 OpenPI 与单臂 SO-101 之间的 6 维状态和动作契约：五个手臂关节加一个夹爪。
- 将 LeRobot 的 `observation.state`、`action`、`observation.images.front` 和文本任务转换为 π0.5 输入。
- 使用固定的 OpenPI 图像槽；缺少腕部相机时填入黑图，并通过 mask 明确标记该图像无效。
- 保留可选的 `images/wrist` 输入，为以后加入腕部相机留出接口。
- π0.5 内部仍使用 32 维 action padding，对外只返回前 6 个 SO-101 动作。
- 检查错误的状态维度、动作维度和相机输入，避免静默训练错误的数据契约。

### 2. SO-101 训练配置与数据变换

文件：

- `src/openpi/training/config.py`
- `src/openpi/training/config_test.py`

主要行为：

- 新增 `pi05_so101_liftcube` 和 `pi05_lora_so101_liftcube` 配置。
- 新增 `LeRobotSO101DataConfig`，将 LeRobot 字段重排为 OpenPI 输入。
- 前五个手臂关节训练为相对动作，夹爪维度保持绝对值；推理输出再恢复成绝对目标。
- 支持通过 `OPENPI_SO101_LIFTCUBE_REPO_ID` 选择本地或 Hugging Face 数据集。
- normalization statistics 使用与数据集相同的 asset ID 加载。
- 测试覆盖配置注册、输入输出 shape、图像槽与 6 维动作往返变换。

### 3. Windows Leader 串口、标定与远程发布

文件：

- `examples/so101/leader_remote_windows.py`
- `examples/so101/REMOTE_LEADER_RUNBOOK.md`
- `.gitignore`

主要行为：

- 为 Windows 上通过 `COM7` 连接的 Feetech STS3215 Leader 提供独立启动入口。
- 支持交互式校准、复用校准缓存、读取关节状态并通过 ZeroMQ 发布。
- 提供安全的 PID/力矩设置流程；真实机械臂的中位姿态和全行程仍必须由操作者观察并完成，代码无法可靠判断现实机械结构是否到位。
- 文档记录 Windows 发布端、SSH reverse tunnel、服务器订阅端和重启后的完整恢复步骤。
- `.gitignore` 排除 `tmp/leisaac-v0.4.0/`、临时 Python 环境和本地运行配置，防止把校准/运行副本误提交进仓库。

### 4. 远程 Leader 仿真验证

文件：

- `examples/so101/remote_leader_sim_smoke.py`

主要行为：

- 在服务器端接收 Windows Leader 的 6 维关节状态。
- 创建 LeIsaac `LeIsaac-SO101-LiftCube-v0` 环境并把 Leader 状态转换为仿真 action。
- 输出首末 action、仿真关节首末位置、最大动作变化和最大关节响应。
- 对完成步数与 reward finite 状态做语义检查，避免只依赖 shell transport exit code。

### 5. 浏览器遥操作与 HDF5 录制

文件：

- `examples/so101/remote_leader_web_teleop.py`

主要行为：

- 提供仅绑定 localhost 的网页预览，显示仿真前置相机画面与运行状态。
- 提供 Start / Resume、Pause、Success + Reset、Discard + Reset 控制。
- 命令经过队列进入仿真线程，网页状态通过 `status.json` 更新，避免 HTTP 线程直接操作 Isaac Sim。
- 默认只预览；传入 `--dataset_file` 后启用 LeIsaac 原生 streaming HDF5 recorder。
- 只把 Success episode 保留为训练数据，并显式关闭 HDF5 recorder，确保文件元数据和缓冲区完整落盘。

### 6. Windows 安全关节保持工具

文件：

- `examples/so101/hold_joint_angles_windows.py`
- `examples/so101/HOLD_JOINT_ANGLES_WINDOWS.md`

主要行为：

- 用于检查通信、读取当前位置和受控地保持指定关节角。
- 包含输入范围、连接和退出保护，减少调试时突然大幅运动的风险。
- 该工具不能替代操作者确认机械臂空间、接线、电源和急停条件。

### 7. LeIsaac HDF5 到 LeRobot 0.4.2 转换

文件：

- `examples/so101/convert_leisaac_hdf5_to_lerobot.py`
- `examples/so101/convert_leisaac_hdf5_to_lerobot_test.py`
- `examples/so101/README.md`

主要行为：

- 只读 LeIsaac 原生 HDF5，只转换标记为成功的 episode。
- 将 USD 弧度坐标按 LeIsaac v0.4.0 的 `action_align=True` 逻辑转换为 SO-101/LeRobot 电机坐标。
- 同时转换 state 与 action，避免二者使用不同坐标系。
- 写入 LeRobot 0.4.2 的 parquet、metadata 和视频目录结构。
- 匹配固定版本的 writer API：任务文本随 frame 写入，不错误地传给 `save_episode()`。
- 支持 `--dry-run`、拒绝覆盖已有输出、可选显式上传；默认不会上传数据。
- 测试覆盖单位换算、成功 episode 过滤、字段 shape 和固定 LeRobot writer API。

### 8. 训练结束时显式释放全部训练资源

文件：

- `scripts/train.py`
- `scripts/train_test.py`
- `src/openpi/training/data_loader.py`
- `src/openpi/training/data_loader_test.py`
- `src/openpi/training/config.py`
- `src/openpi/training/config_test.py`

问题背景：

- 两张 RTX 3090 的 FSDP 一步训练和 step 0 checkpoint 保存已经成功。
- Orbax 日志显示异步保存线程与 finalize 都完成，但 Python 进程退出阶段出现过整机重启。
- 原代码最后只调用 `checkpoint_manager.wait_until_finished()`。该方法只等待未完成的 save，并不关闭 manager 的线程池和内部对象。
- 第一次修复验证后，日志已经打印 `Checkpoint manager closed`，但主机仍在随后的解释器退出阶段异常复位。
- 默认 DataLoader 使用两个 `persistent_workers`；原训练函数不关闭 iterator 或 loader。CPU 训练的 native crash 栈中也出现了两个 multiprocessing queue feeder 线程。

本次修复：

- 在 checkpoint manager 创建后立刻进入 `try/finally`。
- 正常训练结束或训练初始化/执行抛异常时，均调用 `checkpoint_manager.close()`。
- `close()` 自身会等待异步保存结束并关闭内部资源，因此不再使用仅等待的末尾调用。
- 增加 `Closing checkpoint manager` 与 `Checkpoint manager closed` 两条日志，便于精确区分重启发生在 Orbax 关闭之前还是之后。
- 新增回归测试：模拟训练初始化失败，验证 manager 仍然只关闭一次。
- 为通用 DataLoader 增加幂等 `close()`，显式停止 PyTorch persistent workers；RLDS loader 也会在底层 dataset 支持时转发 `close()`。
- 训练退出时按顺序关闭 checkpoint manager、进度条、data iterator、data loader 和 W&B run，并为每一阶段打印开始/完成日志。
- SO-101 full 与 LoRA 配置默认设为 `num_workers=0`。这会牺牲部分数据加载吞吐，但避免在尚未稳定的服务器退出路径中保留 worker 子进程。
- 新增 DataLoader 幂等关闭测试、训练加载失败时的资源关闭顺序测试，以及 SO-101 worker 配置断言。

这些修复消除了已确认的 Orbax 和 DataLoader 清理遗漏，但不能单独证明整机重启一定消失。如果服务器在打印 `Training resources closed` 后仍重启，应继续检查 JAX/XLA、NCCL、NVIDIA 驱动或硬件在多 GPU context teardown 阶段的问题。

### 9. RedCubeToBox 场景坐标审计

文件：

- `examples/so101/red_cube_to_box_scene_audit.py`
- `examples/so101/red_cube_to_box_task/__init__.py`
- `examples/so101/red_cube_to_box_task/env_cfg.py`
- `examples/so101/red_cube_to_box_task/mdp.py`
- `examples/so101/red_cube_to_box_task/state_machine.py`
- `examples/so101/red_cube_to_box_env_smoke.py`
- `examples/so101/red_cube_to_box_expert_smoke.py`
- `examples/so101/README.md`

主要行为：

- S3 先复用已经通过闭环验证的 LeIsaac LiftCube 机械臂、桌面、红方块和前置相机，避免同时更换机器人与场景资产。
- 启动单环境、单次 reset 的有界 headless 审计，输出环境原点、机器人根节点、末端执行器、红方块和相机信息。
- 不连接真实 Leader，不写动作，不录制数据，也不修改下载的 LeIsaac 资产。
- 根据实测坐标把目标盒中心放在 `(0.520, -0.36161)`，即沿方块的 `+X` 方向横向分离，同时维持接近的 `Y` 前伸距离。
- 用五个绿色运动学刚体组成底板和四面墙，不依赖新的外部 USD 资产。
- 五个部件分别使用环境下的独立顶层 prim；这是为了兼容当前 IsaacLab shape spawner，避免嵌套 regex 路径要求尚未创建的 `TargetBox` 父 prim。
- 成功判据同时检查方块位于盒内、低于墙顶并且线速度不超过 `0.15 m/s`，避免方块从盒子上方掠过时误判。
- 有界 smoke 先验证正常 reset 不成功，再把方块临时传送到盒内验证判据为真；传送只用于测试，不属于脚本专家或数据生成轨迹。
- 初版脚本专家使用 LeIsaac 的 `so101_state_machine` 绝对位姿 IK action，按接近、下降、闭合、抬升、横移、入盒、释放、撤离和稳定等待九个阶段运行。
- 专家 smoke 暂时关闭环境自动成功终止与超时，防止检查最终方块位置前自动 reset；它输出阶段切换、最终盒内偏移、速度和成功状态。
- 抓取失败诊断会在每次阶段切换时输出 gripper/jaw/cube 世界坐标、jaw 到 cube 距离、夹爪关节位置、`pick_cube` 判据和该阶段首个 action，以区分 IK 未到位、几何偏移和夹爪未闭合。
- 首次动态诊断在抬升前达到 `0.01826 m` jaw-cube 距离，满足 `0.02 m` 距离阈值，但夹爪停在 `0.45639 rad`，高于 `0.26 rad` 抓取阈值。因此 RedCubeToBox 状态机把通用闭合目标从 `0.4` 改为 `0.05 rad`，不修改已经到位的 IK 抓取位姿。

### 10. SO-101 总说明

文件：

- `examples/so101/README.md`
- `examples/so101/0.originalCodeChanges.md`（本文档）

`README.md` 是操作说明，本文档是相对原始 OpenPI 基线的改动审计。以后修改 SO-101/OpenPI 代码时，应同步更新本文档。

## 已完成的验证

- SO-101 policy/config 单元测试：`10 passed`。
- HDF5 converter 单元测试：`4 passed`。
- OpenPI SO-101 完整 transform pipeline：6 维 action 往返最大误差为 `0.0`。
- 远程 Leader tunnel：服务器成功连续收到 6 个 float 的关节帧。
- 远程 Leader 仿真：完成 `1200` steps，reward 全部 finite，检测到 Leader 动态变化与仿真关节响应。
- 网页遥操作：Start、Pause、Success + Reset、Discard + Reset 与状态显示均验证正常。
- HDF5 录制：1 个成功 episode、`1198` frames，包含前置 RGB、状态、动作和仿真初始状态。
- LeRobot readback：`1198` frames、1 episode、60 FPS，图像/state/action/task 均能读取。
- normalization statistics：`state` 与 `actions` 均存在，训练配置已成功加载。
- 两卡 FSDP：完成 step 0，`loss=0.1311`，checkpoint 异步保存和 finalize 成功。
- 训练资源清理回归测试：目标测试 `6 passed`，覆盖异常路径中的 checkpoint manager、DataLoader worker 和资源关闭顺序。

## 不在 Git 中的运行产物

以下内容属于机器或实验产物，没有提交到 OpenPI 源码仓库：

- Windows Leader 校准缓存和串口运行配置。
- 下载的 LeIsaac USD/纹理资源。
- 原生 LeIsaac HDF5 数据集。
- 转换后的本地 LeRobot parquet、metadata 和视频。
- normalization statistics、训练 checkpoint、W&B 文件和运行日志。
- 服务器上的 LeIsaac/IsaacLab 环境、Conda 包与兼容性约束文件。

这些产物应通过实验记录、数据备份或模型仓库单独管理，不能只依赖 Git 源码恢复。

## 已知限制与下一阶段

- 当前 `1198` 帧、单 episode 数据只用于链路验证，不足以训练有意义的策略。
- RedCubeToBox 已进入 S3：场景审计、目标盒、成功判据和初版脚本专家已实现；动态抓放成功仍需在服务器 smoke 中验证和调参。
- 单张 RTX 3090 已因额外申请约 `5.37 GiB` 显存而 OOM；当前训练应使用两卡 FSDP，并避免无必要的 JAX 大额预分配。
- Orbax manager 已验证能显式关闭，但主机在之后仍异常复位；新增的逐资源清理尚待管理员允许后验证。若仍复位，应保留上一启动的 kernel/BMC/SEL 日志，不能把它当作普通 Python 异常。
- 在获得多条、覆盖足够状态空间的成功轨迹之前，不应把 smoke dataset 的训练结果当作模型能力结论。

## 提交时间线

| Commit      | 日期       | 内容                                                           |
| ----------- | ---------- | -------------------------------------------------------------- |
| `675a7a6` | 2026-08-06 | Add SO-101 pi0.5 training integration                          |
| `4a1650f` | 2026-08-08 | Add safe Windows SO-101 leader helper                          |
| `298d1ad` | 2026-08-08 | Harden Windows SO-101 remote runtime                           |
| `d4a1b1f` | 2026-08-08 | Clarify SO-101 policy server port                              |
| `b25fce3` | 2026-08-08 | Add remote SO-101 simulation smoke                             |
| `ca2606a` | 2026-08-08 | Fix remote Leader smoke lint                                   |
| `fb3ad86` | 2026-08-08 | Report simulated joint response in Leader smoke                |
| `9f93cc7` | 2026-08-08 | Document remote SO-101 restart workflow                        |
| `00c9839` | 2026-08-08 | Add browser preview for remote SO-101 teleop                   |
| `a34bba5` | 2026-08-08 | Fix web teleop import ordering                                 |
| `f983aeb` | 2026-08-08 | Satisfy Ruff import block spacing                              |
| `f0e45f8` | 2026-08-08 | Add safe Windows SO-101 joint hold utility                     |
| `88a01f3` | 2026-08-08 | Fix web teleop button handlers                                 |
| `5fcb69f` | 2026-08-08 | Add optional HDF5 web teleop recording                         |
| `d316533` | 2026-08-08 | Close streaming HDF5 recorder explicitly                       |
| `1980e7a` | 2026-08-08 | Add isolated LeIsaac HDF5 converter                            |
| `75d6db3` | 2026-08-08 | Fix converter test import on Linux                             |
| `9abe075` | 2026-08-09 | Match pinned LeRobot writer API                                |
| `1f81841` | 2026-08-09 | Configure local SO101 dataset override                         |
| `0ffe025` | 2026-08-09 | 显式关闭训练 checkpoint manager，并加入本改动记录              |
| `d1dc235` | 2026-08-09 | 显式关闭 DataLoader/W&B/进度条并禁用 SO-101 persistent workers |
| `8a94a24` | 2026-08-09 | Add RedCubeToBox scene audit                                   |
| `a1dcd23` | 2026-08-09 | Add RedCubeToBox environment scaffold                          |
| `def2e03` | 2026-08-09 | Fix RedCubeToBox prim spawning                                 |
| `4b86307` | 2026-08-09 | Add RedCubeToBox scripted expert                               |
| `bbca9cd` | 2026-08-09 | Add RedCubeToBox grasp diagnostics                             |

## 维护规则

以后对 SO-101/OpenPI 集成做改动时：

1. 在相应章节说明动机、文件、数据契约和行为变化。
2. 写明验证命令及可观察的成功标志，不只记录 shell exit code。
3. 区分源代码、机器配置、校准数据、实验数据和模型 checkpoint。
4. 对可能影响真机安全、数据坐标系或训练恢复的变化明确标注风险。
5. 提交后把 commit hash 和标题追加到时间线。
